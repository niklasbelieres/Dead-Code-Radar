{
  "name": "deadcode-radar",
  "version": "0.1.0",
  "private": true,
  "packageManager": "pnpm@9.15.0",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "clean": "pnpm -r exec rimraf dist",
    "build": "pnpm -r run build",
    "dev": "pnpm -r run dev"
  },
  "devDependencies": {
    "typescript": "^5.5.3",
    "@types/node": "^25.2.0",
    "rimraf": "^6.1.2",
    "ts-morph": "^27.0.2"
  }
}
